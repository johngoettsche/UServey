

procedure readText(file)
	suspend reads(file, text)
end

procedure main(args)
	filename := args[1]
	write(filename)
	file := open(filename, "r")
	text := &letters ++ &digits ++ ';:,.\"'
	every t := readText(file) do write(t)
end